{
    "name": "cinescope",
    "private": true,
    "engines": {
        "node": "24.x"
    },
    "scripts": {
        "start": "node --env-file-if-exists=.env server.js",
        "test": "node --test"
    }
}
